<%= form_for(task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %></br>
  <%= f.label :details %>
  <%= f.text_field :details %></br>
  <% if !task.new_record?() %>
    <%= f.label :completed %>
    <%= f.check_box :completed %></br>
  <% end %>
  <%= f.submit %>
<% end %>
